README

How to use TkAuto with more complex layouts
involving more than a single frame

1. Create a spreadsheet layout for the main containing frame
2. Create spreadsheet layout(s) for any sub frames

For 1 and 2 execute tkauto like this:

    tkauto layout_main.xlsx -o main.py
    tkauto layout_fram.xlsx -o fram.py

Edit the Frame(s) before fitting them into the main.py

Copy the main.py over to the final app name (like: app.py)

Do all of the final touches in app.py

